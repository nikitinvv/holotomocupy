path=/eagle/APS_IRI/vnikitin/20250604/Y350a_FT_large_rand_disp_rec_new          # input directory (steps15 output)
path_out=/eagle/APS_IRI/vnikitin/20250604/Y350a_FT_large_rand_disp_rec_new_fft   # output directory
pfile=Y350a_FT_large_rand_disp_nobin_020nm                                      # scan prefix name

nchunk=8              # projections per GPU batch
ntheta=4000           # number of projection angles
start_theta=0         # index of first projection angle
ndist=1               # number of distances

niter=769             # number of iterations
start_iter=512        # resume from this iteration (0 = start fresh)
checkpoint_step=32    # save checkpoint every N iters (-1 = disabled)
error_step=32         # print error every N iters

nz=2048               # vertical detector size (pixels)
n=2048                # horizontal detector size (pixels)
nzobj=2304            # vertical object size (pixels)  (2048+256 for large rand disp)
nobj=2304             # horizontal object size in x and y (pixels)
bin=1
shift_type=fft

rotation_center_shift=-11.79  # rotation axis offset from detector centre (pixels)
paganin=100           # delta/beta ratio for Paganin phase retrieval
obj_dtype=complex64   # object array dtype: float32 or complex64
mask=1.1              # circular mask radius as fraction of n (-1 = no mask)

lam_prbfit=3.1e-3     # probe fit regularisation weight
lam_laplacian=0       # 3-D Laplacian regularisation weight
rho=1,0.05,0.02       # gradient step scales: object, probe, positions

log_level=INFO
