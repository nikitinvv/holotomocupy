# ---------------------------------------------------------------------------
# DISABLED - step0.py (NFP probe retrieval) does not apply to this dataset.
#
# /eagle/APS_IRI/vnikitin/20250604 holds only
#     Y350a_FT_large_rand_disp_nobin_020nm_1_/
# on the assumption that no *_NFPwS_* companion scan exists -- see the CONFIRM
# note below, which is now in doubt.  Until it is checked: run steps15.py and
# step6.py only, and leave prb_file unset in the config_step6_bin*.conf files.
#
# CONFIRM: this note predates the pfile correction to _nobin_020nm and assumed
# the main scan was a different (006nm) one.  Now that both refer to 020nm, an
# NFPwS companion may well exist for it.  Check with:
#     ls -d /eagle/APS_IRI/vnikitin/20250604/Y350a_FT_large_rand_disp_nobin_020nm*
# If a *_NFPwS_1_ directory is there, uncomment the block below, run step0, and
# set prb_file in the config_step6_bin*.conf files.
#
# path=/eagle/APS_IRI/vnikitin/20250604
# path_out=/eagle/APS_IRI/vnikitin/20250604/Y350a_FT_large_rand_disp_rec
# scan_file=Y350a_FT_large_rand_disp_nobin_020nm_NFPwS_1_/Y350a_FT_large_rand_disp_nobin_020nm_NFPwS_1_0000.h5
# meta_file=Y350a_FT_large_rand_disp_nobin_020nm_NFPwS_1_/ls3231-Y350a-Y350a_FT_large_rand_disp_nobin_020nm_NFPwS_1_.h5
# h5_out=nfp_results.h5
# dataset_ids=1
# nchunk=2
# n=4096
# niter=513
# checkpoint_step=32
# error_step=32
# rho=1,2,0.1
# log_level=INFO
# ---------------------------------------------------------------------------
